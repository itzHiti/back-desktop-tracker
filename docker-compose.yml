version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_DB: desk_tracker
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: h0lypassw0rd!=s1mplepa$$
    ports:
      - "5432:5432"